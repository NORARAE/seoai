<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Meta Description
    |--------------------------------------------------------------------------
    | Used as the fallback meta description for any page that does not define
    | its own. Surfaces in Google SERPs, AI Overviews, and LLM crawlers.
    */
    'default_meta_description' => 'Get found across Google, ChatGPT, and AI — the new way to search. Expand into every city, every service, and dominate local discovery.',

    /*
    |--------------------------------------------------------------------------
    | Default OG Description
    |--------------------------------------------------------------------------
    | Same content as meta description — must stay ≤ 155 chars.
    */
    'default_og_description' => 'Get found across Google, ChatGPT, and AI — the new way to search. Expand into every city, every service, and dominate local discovery.',

    /*
    |--------------------------------------------------------------------------
    | Site Name
    |--------------------------------------------------------------------------
    | Used in og:site_name across all pages.
    */
    'site_name' => 'SEO AI Co™',

    /*
    |--------------------------------------------------------------------------
    | Title Suffix
    |--------------------------------------------------------------------------
    | Appended to page titles where dynamic title generation is used.
    */
    'title_suffix' => '| SEO AI Co™',

    /*
    |--------------------------------------------------------------------------
    | Brand Positioning
    |--------------------------------------------------------------------------
    | Canonical one-line description of the platform — for schema, AI
    | context blocks, and structured data injections.
    */
    'brand_positioning' => 'AI Discovery Infrastructure for Local Service Businesses',

];
